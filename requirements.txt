# Deps pinadas (reprodutível). Gerar de novo com:
#   .venv/bin/pip freeze | grep -iE "^(fastapi|uvicorn|httpx|pydantic|python-dotenv|pystray|pillow)=="
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.5
python-dotenv==1.2.3
pystray==0.19.5
pillow==12.3.0
